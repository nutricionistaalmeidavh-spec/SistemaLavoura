import { createFinancialEntry, summarizeFinancialEntries } from '../shared/packages/domain-finance/src/index.js';export function createCropExpense({id,seasonId,fieldId,amountMinor,description,category}={}){return createFinancialEntry({id,direction:'expense',amountMinor,description,allocation:{kind:'crop-field',id:fieldId},metadata:{seasonId,category}});}export function createHarvestIncome({id,seasonId,fieldId,amountMinor,description='Harvest sale',partyId=null}={}){return createFinancialEntry({id,direction:'income',amountMinor,description,partyId,allocation:{kind:'crop-field',id:fieldId},metadata:{seasonId}});}export function cropFinancialMetrics(entries,{seasonId,fieldId,areaHa=0}={}){const scoped=entries.filter(e=>(!fieldId||e.allocation?.id===fieldId)&&(!seasonId||e.metadata?.seasonId===seasonId)),summary=summarizeFinancialEntries(scoped);return Object.freeze({...summary,costPerHaMinor:areaHa>0?summary.expenseMinor/areaHa:null});}