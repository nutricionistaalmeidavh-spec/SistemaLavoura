const positive=(v,l)=>{if(!Number.isFinite(v)||v<=0)throw new TypeError(`${l} must be positive.`);return v;};export function createSeasonPlan({id,seasonId,fieldIds,expectedYieldPerHa,budgetMinor=null}={}){return Object.freeze({id,seasonId,fieldIds:Object.freeze([...new Set(fieldIds)]),expectedYieldPerHa:positive(expectedYieldPerHa,'Expected yield'),budgetMinor,status:'planned'});}export function scheduleFieldOperation({id,seasonId,fieldId,typeId,scheduledAt,machineAssetId=null,operatorPartyId=null,inputItems=[]}={}){return Object.freeze({id,seasonId,fieldId,typeId,scheduledAt:new Date(scheduledAt).toISOString(),machineAssetId,operatorPartyId,inputItems:Object.freeze(inputItems.map(v=>Object.freeze({...v}))),status:'planned'});}export function startFieldOperation(operation,{startedAt=new Date().toISOString()}={}){if(operation.status!=='planned')throw new Error('Only planned operation can start.');return Object.freeze({...operation,status:'in-progress',startedAt:new Date(startedAt).toISOString()});}export function completeFieldOperation(operation,{completedAt=new Date().toISOString(),actualCostMinor=null,notes=null}={}){if(operation.status!=='in-progress')throw new Error('Only in-progress operation can complete.');return Object.freeze({...operation,status:'completed',completedAt:new Date(completedAt).toISOString(),actualCostMinor,notes});}export function createHarvestLot({id,seasonId,fieldId,quantity,unit,areaHa,harvestedAt}={}){return Object.freeze({id,seasonId,fieldId,quantity:positive(quantity,'Harvest quantity'),unit,areaHa:positive(areaHa,'Harvest area'),harvestedAt:new Date(harvestedAt).toISOString(),yieldPerHa:quantity/areaHa});}export function cropYieldSummary(harvestLots=[]){const qty=harvestLots.reduce((s,h)=>s+h.quantity,0),area=harvestLots.reduce((s,h)=>s+h.areaHa,0);return Object.freeze({quantity:qty,areaHa:area,yieldPerHa:area>0?qty/area:null});}
export function cancelFieldOperation(operation,{reason,cancelledAt=new Date().toISOString()}={}){if(!['planned','in-progress'].includes(operation.status))throw new Error('Only open field operation can be cancelled.');return Object.freeze({...operation,status:'cancelled',cancelReason:reason,cancelledAt:new Date(cancelledAt).toISOString()});}