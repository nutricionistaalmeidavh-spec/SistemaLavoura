import assert from 'node:assert/strict';import {resolveEntitlements} from '../src/entitlements.js';import {capabilityForScreen,capabilityForAction,assertEntitled} from '../src/edition-policy.js';
const cases={essential:{allow:['overview','fields','seasons','operations','inputs','harvest','settings'],deny:['inventory','finance','reports','gis-import','satellite','iot','admin']},management:{allow:['overview','fields','seasons','operations','inputs','harvest','inventory','finance','reports','settings','admin'],deny:['gis-import','satellite','iot']},complete:{allow:['overview','fields','seasons','operations','inputs','harvest','inventory','finance','reports','settings','gis-import','satellite','iot','admin'],deny:[]}};
for(const [edition,matrix] of Object.entries(cases)){const e=resolveEntitlements({edition});for(const id of matrix.allow)assert.doesNotThrow(()=>assertEntitled(e,capabilityForScreen(id),{screenId:id}));for(const id of matrix.deny)assert.throws(()=>assertEntitled(e,capabilityForScreen(id),{screenId:id}),x=>x.code==='EDITION_FORBIDDEN');}
const essential=resolveEntitlements({edition:'essential'});assert.throws(()=>assertEntitled(essential,capabilityForAction('reports','pdf')));const management=resolveEntitlements({edition:'management'});assert.doesNotThrow(()=>assertEntitled(management,capabilityForAction('reports','pdf')));
console.log('Edition QA: OK');
